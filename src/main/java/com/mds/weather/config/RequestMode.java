package com.mds.weather.config;

/**
 * Type work client sdk
 */
public enum RequestMode {
    /**
     * Type work client sdk
     */
    ON_DEMAND_MODE,
    /**
     * SDK requests current weather every half-time expired
     */
    POLLING_MODE
}
